using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Simple dummy timer to use in place of working beat map.
/// Used to import into metronome/AI structure
/// </summary>
public class Timer : MonoBehaviour {
    // dummy variable
    protected float noMusicTimer = 0.5f;
	// Use this for initialization
	void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
